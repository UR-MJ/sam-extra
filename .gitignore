__pycache__/
*.pyc
.venv/
outputs/
models/
*.pt
*.pth
*.safetensors
# Keep shared Claude Code config (settings.json + hooks) tracked; ignore only
# the per-user local overrides.
.claude/settings.local.json
pip

# v0.8.0 — shallow clone of kohya-ss/sd-scripts populated by install.py
anima_vendor/

# v0.9.0 — shallow clone of willmiao/ComfyUI-Lora-Manager populated by install.py
lora_manager_vendor/
# Added by code-review-graph
.code-review-graph/

# Assistant scratch/working directories — never part of the extension.
.codex/
.hallmark/
.agents/
.claude/anima_prompt.json
.claude/anima_workflow.json
